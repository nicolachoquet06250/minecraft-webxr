{
  "name": "minecraft-xr",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && tsc && vite build",
    "preview": "vite preview",
    "build:wasm": "cargo install wasm-pack && cd wasm && cargo test && wasm-pack build --target web --out-dir ../src/assets/wasm"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@babylonjs/core": "^9.10.1",
    "@babylonjs/gui": "^9.10.1",
    "@babylonjs/loaders": "^9.10.1"
  }
}
